name: Release go daemons

on:
  workflow_call:
    inputs:
      start_mongodb:
        description: run mongodb on build step
        type: boolean
        required: false
        default: false
      start_redis:
        description: run redis on build step
        type: boolean
        required: false
        default: false
      build-run-unit-tests:
        description: run unit tests on build step
        type: boolean
        required: false
        default: true
      dockerfile-image:
        description: image for Generate Dockerfile
        type: string
        required: false
        default: ubuntu:22.04
      dockerfile-run:
        description: addication "run" for Generate Dockerfile
        type: string
        required: false
        default: "echo run"
      dockerfile-cmd:
        description: addication "cmd" for Generate Dockerfile
        type: string
        required: false
        default: "echo cmd"
      deploy-on-docker-hub:
        type: boolean
        required: false
        default: true
      deploy-arm:
        type: boolean
        required: false
        default: true
      build-cmd:
        description: addication "cmd" for build step
        type: string
        required: false
        default: "echo cmd"
      matrix-os:
        description: list of OS targets
        type: string
        required: false
        default: '["linux"]'

permissions: write-all

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    strategy:
      matrix:
        arch:
          - amd64
          - arm64
        os: ${{ fromJson(inputs.matrix-os) }}
        go-version:
          - '1.24'
        include:
          - arch: amd64
            rpm_arch: x86_64
          - arch: arm64
            rpm_arch: aarch64
    env:
      GOPRIVATE: github.com/anyproto
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-go@v6
        with:
          go-version: '${{ matrix.go-version }}'
          check-latest: true

      - name: git config
        run: git config --global url.https://${{ secrets.ANYTYPE_PAT }}@github.com/.insteadOf https://github.com/

      - name: Start MongoDB
        if: ${{ inputs.start_mongodb }}
        uses: supercharge/mongodb-github-action@1.12.1
        with:
          mongodb-version: 8.2
          mongodb-replica-set: test-rs

      - name: Start Redis
        uses: supercharge/redis-github-action@1.8.1
        if: ${{ inputs.start_redis }}
        with:
          redis-image: redis/redis-stack-server

      # build {{
      - name: Install dependencies
        run: make deps CGO_ENABLED=0

      - name: Run unit tests
        if: ${{ inputs.build-run-unit-tests }}
        run: make test CGO_ENABLED=0

      - name: Run build
        run: make build CGO_ENABLED=0 BUILD_GOOS=${{ matrix.os }} BUILD_GOARCH=${{ matrix.arch }}
      # }}

      - name: Get release version
        id: release-version
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME##*/}"
          ver="${tag#v}"
          echo "RELEASE_VERSION=${ver}" >> "$GITHUB_OUTPUT"
          echo "RELEASE_VERSION_RPM=${ver//-/_}" >> "$GITHUB_OUTPUT" # FPM converts for RPM

      - name: Addication command for build step
        run: ${{ inputs.build-cmd }}

      # create assets {{
      - name: Create zip archive
        if: matrix.os == 'windows'
        run: |
          zip --junk-paths ${{ github.event.repository.name }}-${{ steps.release-version.outputs.RELEASE_VERSION }}.${{ matrix.os }}-${{ matrix.arch }}.zip bin/*
      - name: Create tar archive
        if: matrix.os != 'windows'
        run: |
          tar \
            --create \
            --gzip \
            --verbose \
            --exclude='.gitignore' \
            --file=${{ github.event.repository.name }}-${{ steps.release-version.outputs.RELEASE_VERSION }}.${{ matrix.os }}-${{ matrix.arch }}.tgz \
            --directory=bin/ \
            .
      - name: Create .deb package
        if: matrix.os == 'linux'
        uses: fb929/github-action-fpm@master
        with:
          fpm_opts:
            --name ${{ github.event.repository.name }}
            --version ${{ steps.release-version.outputs.RELEASE_VERSION }}
            --architecture ${{ matrix.arch }}
            --url ${{ github.repositoryUrl }}
            --description "commit ${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}"
            --exclude '*/.gitignore'
            --exclude '*/.git'
            --input-type dir
            --output-type deb
          fpm_args: ./bin
      - name: Create .rpm package
        if: matrix.os == 'linux'
        uses: fb929/github-action-fpm@master
        with:
          fpm_opts:
            --name ${{ github.event.repository.name }}
            --version ${{ steps.release-version.outputs.RELEASE_VERSION }}
            --architecture ${{ matrix.rpm_arch }}
            --url ${{ github.repositoryUrl }}
            --description "commit ${{ github.server_url }}/${{ github.repository }}/commit/${{ github.sha }}"
            --exclude '*/.gitignore'
            --exclude '*/.git'
            --input-type dir
            --output-type rpm
          fpm_args: ./bin
      # }}

      - name: Debug
        run: |
          ls -al ./

      - name: Upload asset zip | Windows
        if: matrix.os == 'windows'
        uses: actions/upload-artifact@v4
        with:
          name: win-${{ matrix.arch }}
          path: |
            ./${{ github.event.repository.name }}-${{ steps.release-version.outputs.RELEASE_VERSION }}.${{ matrix.os }}-${{ matrix.arch }}.zip

      - name: Upload asset tgz | Unix
        if: matrix.os != 'windows'
        uses: actions/upload-artifact@v4
        with:
          name: nix-${{ matrix.os }}-${{ matrix.arch }}
          path: |
            ./${{ github.event.repository.name }}-${{ steps.release-version.outputs.RELEASE_VERSION }}.${{ matrix.os }}-${{ matrix.arch }}.tgz

      - name: Upload .deb and .rpm assets | Linux
        if: matrix.os == 'linux'
        uses: actions/upload-artifact@v4
        with:
          name: linux-packages-${{ matrix.arch }}
          path: |
            ./${{ github.event.repository.name }}_${{ steps.release-version.outputs.RELEASE_VERSION }}_${{ matrix.arch }}.deb
            ./${{ github.event.repository.name }}-${{ steps.release-version.outputs.RELEASE_VERSION_RPM }}-1.${{ matrix.rpm_arch }}.rpm


  create_release:
    name: Create release
    needs:
      - build
    runs-on: ubuntu-latest
    outputs:
      body: ${{ steps.release_info.outputs.body }}
      html_url: ${{ steps.release_info.outputs.html_url }}
      tag_name: ${{ steps.release_info.outputs.tag_name }}
    steps:
      - name: Download all build artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts

      - name: List downloaded artifacts (debug)
        run: |
          echo "Downloaded artifacts:"
          ls -R artifacts

      - name: Get release version
        id: release-version
        shell: bash
        run: |
          tag="${GITHUB_REF_NAME##*/}"
          echo "RELEASE_VERSION=${tag}" >> "$GITHUB_OUTPUT"

      - name: Create release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          name: Release ${{ steps.release-version.outputs.RELEASE_VERSION }}
          generate_release_notes: true
          draft: false
          prerelease: true
          token: ${{ secrets.ANYTYPE_PAT }}
          files: |
            artifacts/**/*

      - name: Get release info
        id: release_info
        uses: actions/github-script@v7
        with:
          script: |
            const release_id = Number('${{ steps.create_release.outputs.id }}');
            const { data } = await github.rest.repos.getRelease({
              owner: context.repo.owner,
              repo: context.repo.repo,
              release_id
            });
            core.setOutput('body', data.body || '');
            core.setOutput('html_url', data.html_url);
            core.setOutput('tag_name', data.tag_name);


  push-docker-image:
    name: Build Docker image and push to registry
    needs:
      - build
      - create_release
    runs-on: ubuntu-latest
    steps:
      - name: Set deploy-platforms
        run: |
          if [ "${{ inputs.deploy-arm }}" = "true" ]; then
            echo "DEPLOY_PLATFORMS=linux/amd64,linux/arm64" >> $GITHUB_ENV
          else
            echo "DEPLOY_PLATFORMS=linux/amd64" >> $GITHUB_ENV
          fi

      - name: Normalization of the release version
        id: release-version
        shell: bash
        run: |
          tag="${{ needs.create_release.outputs.tag_name }}"
          tag="${tag##*/}"
          ver="${tag#v}"
          echo "RELEASE_VERSION=${ver}" >> "$GITHUB_OUTPUT"

      - name: Set env ARTIFACT_PREFIX
        run: |
          echo ARTIFACT_PREFIX=${{ github.event.repository.name }}_${{ steps.release-version.outputs.RELEASE_VERSION }} >> $GITHUB_ENV

      - name: Download release asset amd64
        uses: dsaltares/fetch-gh-release-asset@1.1.2
        with:
          version: tags/${{ needs.create_release.outputs.tag_name }}
          file: ${{ env.ARTIFACT_PREFIX }}_amd64.deb
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Download release asset arm64
        if: ${{ inputs.deploy-arm }}
        uses: dsaltares/fetch-gh-release-asset@1.1.2
        with:
          version: tags/${{ needs.create_release.outputs.tag_name }}
          file: ${{ env.ARTIFACT_PREFIX }}_arm64.deb
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Generate Dockerfile
        run: |
          cat <<EOF > Dockerfile
          # syntax=docker/dockerfile:1
          FROM ${{ inputs.dockerfile-image }}
          LABEL org.opencontainers.image.source=${{ github.repositoryUrl }}
          LABEL org.opencontainers.image.description=https://tech.anytype.io
          LABEL org.opencontainers.image.licenses=MIT
          ARG TARGETARCH
          COPY ${{ env.ARTIFACT_PREFIX }}_\${TARGETARCH}.deb .
          RUN apt-get install -y ./${{ env.ARTIFACT_PREFIX }}_\${TARGETARCH}.deb && rm -f ./${{ env.ARTIFACT_PREFIX }}_\${TARGETARCH}.deb
          RUN ${{ inputs.dockerfile-run }}
          RUN rm -rf /var/lib/apt/lists/*
          EXPOSE 443
          EXPOSE 8000
          EXPOSE 8080
          CMD ${{ inputs.dockerfile-cmd }}
          EOF

      - name: Debug
        run: pwd; cat Dockerfile;  ls -R

      # push to registry
      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to GitHub Container Registry
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Push to GitHub registry
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: ${{ env.DEPLOY_PLATFORMS }}
          push: true
          tags: |
            ghcr.io/${{ github.repository }}:latest
            ghcr.io/${{ github.repository }}:v${{ steps.release-version.outputs.RELEASE_VERSION }}
          outputs: type=image,name=target,annotation-index.org.opencontainers.image.description=https://tech.anytype.io

      - name: Login to Docker Hub
        if: ${{ inputs.deploy-on-docker-hub }}
        uses: docker/login-action@v3
        with:
          registry: docker.io
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Push to Docker Hub registry
        if: ${{ inputs.deploy-on-docker-hub }}
        uses: docker/build-push-action@v6
        with:
          context: .
          file: Dockerfile
          platforms: ${{ env.DEPLOY_PLATFORMS }}
          push: true
          tags: |
            docker.io/${{ github.repository }}:latest
            docker.io/${{ github.repository }}:v${{ steps.release-version.outputs.RELEASE_VERSION }}
          outputs: type=image,name=target,annotation-index.org.opencontainers.image.description=https://tech.anytype.io


  notify:
    needs:
      - build
      - create_release
      - push-docker-image
    if: ${{ always() }}
    runs-on: ubuntu-latest
    steps:
      - name: Notify Slack (workflow result)
        uses: ravsamhq/notify-slack-action@v2
        with:
          status: ${{ (contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')) && 'failure' || 'success' }}
          notification_title: "${{ (contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')) && '❌' || '✅' }} {workflow} {repo}"
          message_format: |
            *Release:* <${{ needs.create_release.outputs.html_url }}|${{ needs.create_release.outputs.tag_name || 'none' }}>
            *Description:*
            ${{ needs.create_release.outputs.body || 'no changes, build skipped' }}
          footer: "Linked Repo <{repo_url}|{repo}> | <{run_url}|View Workflow Run>"
        env:
          SLACK_WEBHOOK_URL: ${{ (contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')) && secrets.SLACK_WEBHOOK_URL_ALERTS || secrets.SLACK_WEBHOOK_URL_RELEASE_TRAIN }}
